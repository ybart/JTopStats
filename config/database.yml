defaults: &defaults
  adapter: sqlite

development:
  database: db/JTopStats_development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/JTopStats_test.db
  <<: *defaults
production:
  database: db/JTopStats_production.db
  <<: *defaults
