defaults: &defaults
  adapter: postgresql

development:
  database: jtop_dev
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: jtop_test
  <<: *defaults
production:
  database: db/JTopStats_production.db
  <<: *defaults
